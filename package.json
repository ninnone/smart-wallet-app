{
  "name": "finalfiwebarch",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve ./public",
    "build": "npm run prepare:build && npm run build:css && npm run copy:assets",
    "prepare:build": "node -e \"const fs=require('fs');const path=require('path');const dir=path.join(__dirname,'public','assets','css');if(!fs.existsSync(dir)){fs.mkdirSync(dir,{recursive:true});}\"",
    "build:css": "node scripts/build-css.js",
    "copy:assets": "node scripts/copy-assets.js",
    "dev": "node scripts/build-css.js && npx tailwindcss -i ./configs/input.css -o ./public/assets/css/main.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.23",
    "cssnano": "^7.1.2",
    "postcss": "^8.5.6",
    "serve": "^14.2.5",
    "tailwindcss": "^3.4.19",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  }
}